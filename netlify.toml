# Simple configuration for static HTML site
[build]
  # No build command needed for plain HTML
  publish = "."

# Prevent caching
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
    Pragma = "no-cache"
    Expires = "0" 